# 交接文档 HANDOFF

> 交接时间：2026-09-11（第二次交接）　项目：agentskill（量化自动交易）
> 本文件供下一个 agent 快速接手，聚焦 **UIA 版同花顺强制下单** 的最新进展与未完成事项。
>
> 📌 **2026-09-13 已对第四节「下一步待办」逐条对代码核实**，其中第 3 条原描述**已过时**
> （撤单链路其实已实现，缺的是实测而非代码）、第 4 条补了一处 `Order` 语义不一致。
> 读第四节时以**核实后的版本**为准。

> ⚠️ **本文件只覆盖 ETF 轮动线 + 同花顺下单链路。**
> 2026-09-12 新增了**个股多因子模型**线，交接见 **`docs/HANDOFF_个股多因子.md`**
> （研究报告见 `docs/个股多因子模型报告.md`）。两条线独立，别混。

## 一、本次会话结论：**卡点已解决，强制下单成功**

上一版 HANDOFF 判断「同花顺自绘输入框不接受程序化注入，强制下单无法成交」——**该判断是错的**。

**真正原因**：同花顺经典版同时开着**两个独立进程窗口**，旧实现认错了窗口。

| 窗口标题 | 性质 |
|---|---|
| `同花顺(9.60.61) - 自选股`（含子窗口 `- ETFs分时走势`） | **行情**窗口，全屏、纯自绘。UIA 只能看到影子控件，`set_value`/`WM_SETTEXT` 全部无效 |
| `网上股票交易系统5.0` | **交易**窗口，约 1118×637，**标准 MFC**，控件句柄齐全 |

旧 `connect()` 用 `startswith("同花顺")` 匹配，抓到了**行情窗口**，于是整条链路上
「找不到输入框 → 填不进去 → 点不动 → 委托不上盘」全都由此而来。
真正的交易窗口一直都在，而且是可完全自动化。

## 二、实测证据（本会话）

```
提交前： cash <金额略>   frozen 0.00
submit    → status: submitted
提交后： cash <金额略>   frozen <金额略>        # 精确冻结 100股 × 3.44
当日委托：13:35:33 510880 红利ETF华泰柏瑞 买入 全部成交 100 股 @ <价格略> 合同号<略> 上海A股
```

- 「委托确认」弹窗正文核对无误：`证券代码：510880(红利ETF华泰柏瑞) / 买入价格：3.427 (卖一) / 买入数量：100`
- 「提示」弹窗：`您的委托已成功提交，合同编号：625377491。`
- 资金侧：`总资产 ≈ 可用金额 + 股票市值` 恒等式成立，`fetch_balance()` 直读控件，**不用 OCR**。

## 三、已完成的改动

| 文件 | 状态 | 说明 |
|---|---|---|
| `trader/ths_uia.py` | **重写** | 全文重写为 win32 消息级实现，见下 |
| `test_uia_order.py` | **重写** | 新增 `--dry`（只填表+截图不提交）；输出写 `uia_order_run.log` |
| `docs/ths_trade_window.md` | **新增** | 完整控件地图 + 踩坑记录，改代码前必读 |
| `tools/ths_dump_tree.py` | **新增** | 控件树导出工具，排障用 |
| 本目录 `diag_*`/`probe_*`/`verify_*` 临时脚本 | 已清理 | 避免污染仓库 |
| `uia_order_submitted.png` | 留档 | 成功下单后的界面截图 |

### `trader/ths_uia.py` 关键设计

1. `connect()` 按标题前缀 `网上股票交易系统` 定位**交易**窗口（不是行情窗口）。
2. 控件一律**按控件 ID** 解析句柄（切页后 HWND 会重建，不能缓存）：
   代码 `1032` / 价格 `1033` / 数量 `1034` / 提交 `1006` / 重填 `1007` / 名称 `1036` / 可买 `1018`
   —— **买入页与卖出页 ID 完全一致**。
3. **填值**：`WM_SETTEXT` / `SetWindowTextW` 对同花顺输入框**无效**（只追加不清空），
   必须用 `PostMessageW(WM_CHAR, 8)` 退格清空 + 逐字符 `WM_CHAR` 输入。
   注意 `WM_GETTEXT` 也不可信（价格/数量框回读为 `''`），**别用回读判断成败**。
4. **提交**：`PostMessageW(hwnd, BM_CLICK, 0, 0)`，不需要窗口置顶、不需要焦点、不怕被遮挡。
5. **委托确认弹窗是交易窗口的 owned 顶层窗口**（`GetWindow(h, GW_OWNER)` 匹配），
   **不在 `GW_CHILD` 子树里** —— 用 `EnumChildWindows` 找不到，这是本次踩的最大坑。
   按钮为标准 `Button`：`是(&Y)` / `否(&N)`，同样 `BM_CLICK`。
   **提交前会解析弹窗正文核对代码/数量，不符则自动点「否」**，防止脏数据上盘。
6. `fetch_balance()` 直接读顶栏 Static（ID 1012/1013/1014/1015/1016/1017），零 OCR。
7. 切页：顶层页走 F1~F4 加速键（PostMessage 到主窗口）；查询子页走 UIA `TreeItem`
   `select()` + 真实鼠标点击（这一步需要窗口未被遮挡）。

## 三·补 2026-09-11 下午：开启今日实盘链路的四个真 bug（已修）

跑 `main.py simulate --ths` 前逐层排查，发现并修掉 4 个会导致**错误下单**的缺陷：

| # | 文件 | 问题 | 修法 |
|---|---|---|---|
| 1 | `trader/ths_uia.py` | `reconcile()` 把持仓存成 6 位 `510880`，而全系统用 `510880.SH` → **市值恒为 0**，引擎误判空仓重复买入 | 新增 `_guess_full_code()`，持仓补全市场后缀 |
| 2 | `scheduler/runner.py` | `run_once` 是**下完单才对账**，本地残留的「幽灵持仓」会被直接拿去下单 | 改为**下单前先 reconcile**；对账失败抛错拒单 |
| 3 | `trader/execution.py` | 只把 `status=="filled"` 的订单加进返回列表，真实 broker 返回 `submitted` → **订单不落库** | `filled` / `submitted` 都返回 |
| 4 | `scheduler/runner.py` | `set_state('strategy_since')` 没加 `dry_run` 保护 → **试算会推进调仓计数器**，几次试算就把调仓日提前 | 加上 `if not self.dry_run` 保护 |

### 另一个隐蔽陷阱：本地账本污染组合风控
`state/trading.db` 是 PaperBroker 时代的账本，净值历史里有 **348385 / 350750** 的假记录。
`PortfolioRisk.scale()` 取 `max(nav_history)` 当峰值 → 算出假回撤 **42%** → 熔断 `scale=0`
→ **所有目标权重归零 → 全清仓**。
处理：旧账本备份为 `state/trading_paper_backup_20260911.db`，`main.py reset` 后以真实账户重建基线。

### OCR 读取的可靠性加固
`ImageGrab` 抓的是**屏幕像素**，同花顺被遮挡时会 OCR 到别的窗口 → 持仓读成空仓 → 重复买入。
已加：`_read_grid_text` 先置顶窗口 → `fetch_position` 重试 3 次 →
`reconcile` 守卫「**有持仓市值却读不到持仓明细 = 读取失败，拒绝用空持仓覆盖账本**」。

### 新增能力
- `trader/broker.py::DryRunBroker`：查询/对账转发真券商，`submit()` 只记录不下单。
- `main.py simulate --ths --dry`：**只读试算**（对账 + 算单，不提交、不落库、不推进计数器）。
- `cmd_simulate` 现在会打印「风控后目标权重」，便于下单前核对。

### 数据链路（务必记住）
**32 位 Python 没有 akshare**，行情刷新必须用 **64 位 `E:\Python\python.exe`**。
标准流程：64 位刷数据 → 32 位下单。已刷到最近收盘日 **20260910**。

### 今日（2026-09-11）落地结果
- 用户决定：A股 T+1，今日买入的股票不可卖；**今日不开新仓**，保持 5 日调仓节奏。
- 账本基线：现金 **<金额略>** / 持仓 **510880.SH <持仓略>** / 总资产 **<金额略>**
- `strategy_since = 4`（显式设定：**下一次运行即调仓日**，约 0914）
- 试算信号（若调仓会买）：511010 国债ETF 30%、510880 红利ETF 2.84%、518880 黄金ETF 1.65%

## 三·补二 2026-09-11 14:22 首次自动量化买入实测（重要经验）

用户指令「使用自动量化买入今日选股」，执行 `main.py simulate --ths`，**3 笔委托全部提交成功**：

| 标的 | 委托 | 结果 |
|---|---|---|
| 510880 红利ETF | 买 <持仓略> @ <价格略> | **全部成交**，均价 <价格略>（累计 <持仓略>，达成 2.84% 目标） |
| 511010 国债ETF | 买 <持仓略> @ <价格略> | **部分成交 100**，余 <持仓略> 挂单 |
| 518880 黄金ETF | 买 <持仓略> @ <价格略> | **未成交** |

### 关键发现 1：模拟盘的撮合要求「委托价 ≤ 当日最高价」
三笔的对照非常干净：

| 标的 | 委托价 | 当日最高 | 结果 |
|---|---|---|---|
| 510880 | 3.473 | 3.480 | 全部成交 |
| 511010 | 141.185 | 141.185 | 部分成交 |
| 518880 | 9.083 | **8.963** | 0 成交 |

系统用「上一交易日收盘价」当限价。遇到今日大跌，委托价就落在**当日成交区间之外** →
挂一整天不成交。**已修**：`UiaThsBroker(use_market_price=True)` 默认不写价格框，
交给客户端「价格跟随」带入**当前最新价**；并用 `parse_confirm_text()` 从委托确认弹窗
回读真实报单价写回 `order.price`。

### 关键发现 2：模拟盘的「撤单页」可撤列表为空 → 无法撤单
`_switch("withdraw")` 能切到撤单页，但页面表格始终为空，`全撤/撤买/撤卖` 按钮
点下去无任何反应（无确认弹窗、冻结资金不变）。**该模拟盘不支持程序化撤单**，
不要指望「撤单重下」来补救。未成交委托会在收盘后自动失效并解冻资金。

### 关键发现 3：`SetForegroundWindow` 对同花顺无效
`SetWindowPos(HWND_TOPMOST→NOTOPMOST)` 不会让交易窗口成为前台窗口
（Windows 禁止后台进程抢前台）。因此：
- `keybd_event`（含 F1~F4 快捷键）会送到**当前前台窗口**，不是同花顺 → **不可用**
- 坐标点击（`SetCursorPos` + `mouse_event`）**可用**，与前台无关
→ 切页必须走「坐标点击左侧树节点」，F 键加速键不可靠。

### 关键发现 4（已修）：冻结资金必须计入净值
下单瞬间资金从「可用」转到「冻结」，若净值只算 `cash + 市值`，总资产会瞬间掉 <金额略>
→ 相对历史峰值算出 **22% 假回撤 → 熔断 → 全清仓**。
已修：`PortfolioAccount` 增加 `frozen`，`total_equity = cash + frozen + 市值`；
`reconcile()` 写入冻结金额；`ExecutionEngine` 仍只用 `cash`（可用）作为购买力 —— 两者不能混用。

### 本次新增能力
- `UiaThsBroker(use_market_price=True)`：默认按客户端带入的当前价下单（推荐）
- `parse_confirm_text()`：解析委托确认弹窗 → code/name/price/qty/amount
- `UiaThsBroker.cancel_all(scope)`：撤单（**在该模拟盘上实际无效**，保留以备接入券商柜面）
- `runner.run_once`：下单后逐笔 `sync_fill()` 回读真实成交，修正台账（原来全是「已报 0 股」）

### 关键发现 5：持仓表 OCR 会读串列，必须用行情价交叉校验
实测两次坏数据：
- `518880` 成本价 `8.950` 被读成 **`8950`**（丢小数点）→ 止损判定变成 -99.9% → **会直接清仓**
- `510880` 那一行被漏读 → 持仓从 3 个变 2 个 → 引擎以为少了仓位

现在的防线（`UiaThsBroker._pos_ok` + `fetch_position(validator=...)`）：
1. **连拍多帧，以「市值自洽」作为停止条件** —— 读到可信的那一帧为止（单帧 OCR 约 1/3 概率出错）
2. 每行**成本价**与行情价偏差 >50% 即判读错，退回行情价
3. 用**行情价 × 数量** 与资金栏「股票市值」交叉核对，**容差 5%**
   （20% 太松：漏读一整行只差 8.4%，会混过去）
4. **持仓凭空消失**判据：本地原持仓在新读结果里没了但账户仍有市值 → 判漏读拒收
5. `parse_position` 丢弃数字个数 <4 或数量 ≤0 的碎片行

> 因此 `reconcile()` 必须拿到 `prices` 才能可靠工作，`runner.run_once` 已改为
> **先算行情价 → 再对账 → 再下单**（对账失败直接抛错拒单）。

### 收尾状态（2026-09-11 14:40 落库）
- 持仓：**510880.SH** / **511010.SH** / **518880.SH**（各 <持仓略>，成交价 <价格略>）
- 现金 <金额略>（冻结已解），总资产 **<金额略>**
- **组合已按目标权重建成**（国债 30% / 红利 2.84% / 黄金 1.65%），复跑试算为「无订单」
- `strategy_since = 5` → 下一个调仓日在 4 次运行之后

## 三·补三 2026-09-11 14:55 无人值守链路打通（重要）

用户追问「每次都要打开 AI 才能买卖，这自动化是不是烂尾」。核查后确认**两个结构性缺口**，
现已补齐，并**实测无人值守跑通**。

### 缺口 1：定时任务从来就没装上
`schtasks /query /tn QuantPanorama_DailyRun` → **不存在**。也就是说
「每日 14:50 自动购入」从未真正发生过，此前全是人工手动跑。

### 缺口 2：`daily_run.bat` 刷不了数据
旧 bat 只有一行 `E:\Python32\python.exe main.py simulate --ths`。
但 **32 位 Python 没装 akshare**，`DataStore.refresh()` 会**静默跳过**下载
→ 定时任务会拿陈旧缓存下单。

### 修复
1. 新增 `tools/refresh_data.py`：用 **64 位** Python 刷行情（支持指定池名）。
2. 重写 `daily_run.bat` 为两步：
   ```
   E:\Python\python.exe   -u tools\refresh_data.py     >> state\daily_run.log 2>&1
   E:\Python32\python.exe -u main.py simulate --ths    >> state\daily_run.log 2>&1
   ```
   两个 python 都加 `-u` + `PYTHONUNBUFFERED=1`：**定时任务中途失败也能留下可用日志**
   （最初没加时，任务退出码 255 而日志里交易步骤一个字都没有，无从排查）。
3. 新增**数据新鲜度守卫**（`scheduler/runner.py`）：拿 `calendar.latest_closed_trading_day()`
   比对 panel 的最后一根 K 线，**数据没刷到最近已收盘交易日就直接拒单**，而不是拿旧数据交易。
4. 重写 `install_schedule.bat` 并注册任务成功：
   - 名称 `QuantPanorama_DailyRun`，周一~周五 **14:50**，State=Ready
   - **普通用户权限即可**（消息级自动化不需要前台焦点，无需 `/rl HIGHEST`）

### 实测结果（无人值守）
```
==================== 周五 2026/09/11 14:54:46 ====================
[refresh] 池=ETF全球 刷新 0 只；最新交易日 ['20260910']   ← 11 只全部 20260910
[runner] 下单前对账完成：现金 <金额略> 冻结 <金额略>
         持仓 {'510880.SH': 1600, '511010.SH': 400, '518880.SH': 300}
【本次非调仓日】不重新选股、不清仓，仅记录净值。
账户：现金 <金额略>  市值 <金额略>  总资产 <金额略>
[done] exit=0
```
`Get-ScheduledTaskInfo`：**LastResult=0（成功）**，NextRun=2026-09-14 14:50。

### 卖出链路：已零风险验证
在没有真下单的前提下走完整条卖出路径 —— 切卖出页 → 填表（名称自动带出）→
点「卖出」→ 弹出确认框 → **点「否」放弃**。确认框内容正确：
`卖出价格：<价格略>(买一) / 卖出数量：100 / 预估金额：<金额略>0`，当日委托未新增。
→ 卖出代码路径可用，**但真实卖出成交尚未发生过**（T+1，且至今没有触发过卖出信号）。

### 告警与体检（2026-09-11 补齐）
原来的问题：失败只在 `state/daily_run.log` 留一段日志，**无人值守时没人会看**，任务会"悄悄不跑"。

现在三层防线：

1. **`tools/daily_job.py`（每日入口，32 位 Python）** —— 取代 bat 直接调 main.py
   - 自己捕获并转发子进程输出，写结构化状态 `state/last_run.json`：
     `{ts, date, ok, exit_code, rebalanced, summary, error}`
   - 失败时写显眼的 `state/ALERT.txt`（含可能原因 + 本次日志尾部），**成功时自动清除**
   - 失败时弹 Windows 消息框提醒
   - 非零退出码向上传递（Task Scheduler 的 `LastTaskResult` 能看到）
   - 自检开关 `--simulate-failure`：不真交易、直接走失败分支，**用来验证告警链路本身有效**
     （报警器要定期按一下，否则坏掉了也不知道）
2. **`tools/check_alerts.py`（体检）** —— 一句话看状态，退出码 0=正常 / 1=有问题
   - 判据：最近一次是否成功；**今天是交易日且已过 14:50+20 分钟却仍无当天运行记录 → 判「到点没跑」**
   - 输出人类可读简报，可直接给人看也可给自动化读
3. **WorkBuddy 定时巡检**（名字「agentskill 自动交易巡检」，工作日 15:10）
   - 跑 `check_alerts.py`；有问题就报告原因+修复办法，正常则只回一句摘要
   - **异常时自动发告警邮件**：Agent Mail（智能体邮箱）别名
     **`<邮箱略>`**，主题 `[agentskill] 自动交易异常 YYYY-MM-DD`
   - 巡检内调用 `SendMessage` 时带 `skip_confirmation=true`（用户已就「每日自动交易异常告警」
     这一用途明确授权；如需收回，改巡检提示词即可）
   - **明确禁止巡检自行下单或改持仓**，任何下单都需人工确认

> 注意：邮件是由 **WorkBuddy 的巡检**发的，不是 `daily_job.py` 发的 ——
> 计划任务里的普通 Python 脚本无法调用 MCP 工具。所以邮件告警依赖
> 「电脑开机 + WorkBuddy 在运行 + 到 15:10 巡检能跑」。
> 若要「脚本自己就能发邮件」，需要给它配一个 SMTP 账号。

### 邮件通道当前状态（2026-09-11）
- 账号 `<邮箱略>` 已绑定、日配额 50 没用过 → **账号侧完全可用**
- 测试邮件曾计划发出，但在 **当前会话** 用 `mcp__agent-mail__SendMessage` 试发时：
  无论 `to` 写成 array of object 还是 JSON 字符串，都被工具的 schema 校验拒掉
  （错误 `-32602`，`field_name: to`，与模型/账号无关）。
- **下次会话不要再尝试调用 `SendMessage`** —— 这是 tool 自身的解析 bug。
- 用户决定：等**工作日 15:10 的自动巡检自然调用一次同一工具**做端到端验证。
  仍撞墙 → 报 bug 给 plugin 方；顺利发出 → 通道顺带验证。
- 走 SMTP 兜底的备份方案：用户需提供「发件 SMTP + 授权码」（暂未提供）。

### 仍存的硬约束（别宣称「全自动」）
1. **同花顺客户端必须开着且已登录**（关掉/电脑睡眠该次运行必失败）。这是同花顺的硬约束。
2. **告警分两级**：
   - 本机（不依赖 WorkBuddy）：`state/ALERT.txt` + Windows 消息框
   - 远程（依赖 WorkBuddy 在运行）：巡检发的告警邮件到 `<邮箱略>`
   若电脑关机，两级都不会响 —— 这属于物理限制。
3. **撤单在模拟盘不可用**（收盘自动失效），撤单重下这条路走不通。
4. 切页依赖坐标点击：THS 窗口被遮挡可能失败 → 会抛错拒单（安全，但需人工看日志）。

## 四、下一步待办

1. **验证卖出链路**：控件 ID 与买入页相同，但尚未实盘跑过一笔卖出，建议用自有持仓小单验证。
2. **`fetch_today_orders` / `fetch_position` 仍依赖 OCR**：表格是 `CVirtualGridCtrl`（自绘，UIA 无子项），
   现方案是「精确定位表格控件矩形 → 截图 → OCR → 按列序解析」。已修正列序
   （委托时间/代码/名称/操作/状态/委托数量/成交数量/委托价格/成交均价/撤消数量/合同编号/市场），
   但仍受行高/字号影响，**可信度低于资金直读**。若要求高，可研究右键「导出」功能。
3. ~~**撤单链路未实现**~~ → **2026-09-13 核实：代码里已实现，原描述过时。**
   实况（以代码为准）：

   - `UiaThsBroker.cancel_all(scope="all")` 已存在（`trader/ths_uia.py:520`），
     `scope` 支持 `all/buy/sell`，控件 ID 已写入 `_CANCEL_IDS`：
     全撤=30001、撤买=30002、撤卖=30003、单笔=1099（与原文一致）。
   - 原文建议的"锁定活动页控件"**已经落地**：`_active_page()`（`:176`，按
     `IsWindowVisible` 选显示中的 `#32770` 页）、`_ctrl(cid, page=, visible_only=)`
     （`:219`）、以及 `cancel_all` 内的可见性兜底（`:530-533`）。
   - **真正缺的不是代码，是验证**：本会话只实盘验证过**买入**链路，
     `cancel_all` 从未在真实客户端上跑过。要收尾应做的是**下一笔可撤委托实测撤单**，
     而不是重写实现。

   ⚠️ 另一条链路 `trader/broker.py::ThsBroker`（easytrader 0.23.7）**没有 cancel 方法**，
   但它已被 `UiaThsBroker` 取代（`main.py:_make_broker` 的 `--ths` 走 UIA），故不影响。
   若要彻底清理，可给 `ThsBroker` 补 `cancel` 或标注弃用。

4. **`scheduler` / `pipeline` 接入新 Broker —— 部分待办，且有一处语义不一致需核对。**
   2026-09-13 核实：

   - `scheduler/runner.py:19` 仍 `from trader.broker import ThsBroker`（旧类），
     但 `runner` 对 broker 是**鸭子类型**调用（`hasattr(broker, "reconcile"/"sync_fill")`），
     而 `UiaThsBroker` **两个方法都有** → **功能上可接**，只是导入行与默认路径仍指向旧类，
     需清理并实测确认。
   - ⚠️ **`Order` 语义确实不一致**：`ThsBroker.submit()` 会置
     `status="filled"` / `filled_price` / `filled_qty` / `fee`；
     而 `UiaThsBroker.submit()`（`:507`）**直接 `return order`，不改任何字段**。
     下游若读 `order.status` 判断成交，两条链路行为不同 —— **这是接入前必须核对的第一件事**。
     需确认 `execution.py` / `scheduler` 是按 `status` 还是按 `sync_fill()` 回读判定成交。
5. **权限**：本次全程以**普通权限**跑通（消息级方案对 UIPI 不敏感）。仅「真实鼠标点击切查询子页」
   一步要求窗口可见；若日后改用真实点击做全部操作，则 Python 与同花顺需同权限。
6. **测试留痕**：本会话共下了 3 笔 510880 买单（各 100 股），**全部成交**，模拟盘因此持有
   510880 <持仓略>（市值 <金额略>）。卖出链路尚未验证，若要清仓需人工或先补测卖出。

## 五、环境与依赖

- Python：**32 位** `E:\Python32\python.exe`（3.10.11），已装 `pywinauto 0.6.6`、`pywin32 312`、
  `Pillow 12.3.0`、`pytesseract 0.3.13`、`numpy`、`pandas`。
- OCR：`E:\Tesseract-OCR\tesseract.exe` + `chi_sim`。
- 同花顺：经典版模拟炒股 9.60.61，客户端 `D:\同花顺软件\同花顺\xiadan.exe`，需已登录模拟会话。
- 运行示例：
  ```bash
  E:/Python32/python.exe test_uia_order.py --dry   # 只填表，不提交
  E:/Python32/python.exe test_uia_order.py         # 真实提交 100 股 510880 买单
  E:/Python32/python.exe tools/ths_dump_tree.py    # 排障：导出控件树 + owned 弹窗
  ```

## 六、其他须知

- git 仓库根在 `agentskill/`（不是上层 `量化/`），分支 `master`。
- 本分支本地改动未推送：`trader/ths_uia.py`(M)、`test_uia_order.py`(M)、
  `docs/ths_trade_window.md`(A)、`tools/ths_dump_tree.py`(A)。

## 七、风险提示

- 同花顺客户端升级可能改变控件 ID / 弹窗结构；升级后先跑 `tools/ths_dump_tree.py` 复核。
- 自动化下单前务必确认是**模拟盘**；切换实盘前需人工复核资金与持仓，并保留人工确认环节。
- 输入框「追加语义」是最大的数据风险源，`_fill_order_form()` 里的「先点重填复位」不能省略。
