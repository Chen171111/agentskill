# 运行产物
# 注意：tools/ 是**源码**（抓取脚本、回测脚本、扫描脚本），必须入库。
# 2026-09-13 修正：原先误把 tools/ 一起忽略，导致新增脚本不进仓库、
# 从 git 接手的人只能看到文档看不到代码。
data/
results/
state/
__pycache__/
*.pyc
*.db
*.sqlite3
*.parquet.partial

# 环境
.venv/
venv/
.env

# 邮件告警配置（**含邮箱授权码，绝不入库**）
# 模板见 config/mail_alert.example.json
config/mail_alert.json

# IDE
.idea/
.vscode/
*.swp

# 运行时日志
debug.log
# 同花顺下单测试的运行时产物（重跑 test_uia_order.py 即可复现，不入库）
uia_order_run.log
uia_order_submitted.png
