# 新会话开场提示词（复制下面代码块）

> 用途：开新对话时把下面整段贴进去，新会话即可完整接手，不需要你再解释背景。
> 最后更新：**2026-09-18** ｜ 与 `docs/HANDOFF_项目总结与下一步.md` 保持一致（后者是唯一入口文档）

---

## 一、直接复制这段 👇

```
接手一个 A 股量化项目，路径 E:\MyWorkAndProject\量化\agentskill（git master，公开仓库）。

请先按顺序读完这 6 份（不要跳过）：
1. docs/HANDOFF_项目总结与下一步.md   ← **唯一入口交接文档**，先读这个
2. tools/README.md                    ← 脚本索引（86 个）+ **20 条方法论铁律**（每轮都该先读）
3. docs/项目运行手册.md                ← 怎么跑 / 预期输出 / 报错处理
4. docs/个股线_股息率实盘方案.md        ← 个股线定稿（执行规则）
5. docs/项目路线图_长期迭代.md          ← P0~P4 阶段、优先顺序、验收标准
6. docs/需求规格_自优化闭环模块.md      ← M1~M4（含实现状态与验收结果）

项目状态（2026-09-18，整体完成度 ≈ 80%，**未完工**）：
- **ETF 轮动线**：实盘在跑（同花顺模拟盘，工作日 14:50 计划任务）。09-18 14:50 运行 ok。
  09-18 21:02 有一次**额外**运行失败（`win32gui` 瞬时缺失）→ **2026-09-19 已确认根因消失、告警归档清理**（HANDOFF §5.1-14）。
  全区间 3.33% / 夏普 0.47 / 回撤 −9.28%；但**跑输等权买入持有 6.92pp**（已量化的事实）。
- **个股线·股息率**：研究定稿 + **纸面跟踪中**，未上实盘。correct 口径样本外（税后·10万真实）**7.45%**，
  同期中证1000 4.03% → 超额 +3.42pp。
- **自优化闭环 M1~M4**：新建，A1~A7 验收全过。
- 4 条已证伪路线（个股多因子/短线/通达信社区公式/精灵大单）已归档，**别再走**。

⚠️ 最该接的活（P0 收尾，见 HANDOFF §4.1）—— **2026-09-18 晚进度**：
1. ✅ **重跑基本完成**：R7~R9 + R10~R18 + R20~R24（N/hold 敏感性、层层加码、IC、
   无税阶梯、叠加税后、`hyst`×行业配额、阈值邻域）→ **共 31 步**，`$PY tools/run_reruns.py --list` 看状态。
   ✅ **31 步全部跑完**（含 R19 `entry` 细网格、R25~R31 阈值/邻域）
2. ✅ **回灌文档 + 摘横幅**：已回灌 **15 份**，**只剩 `个股线_股息率策略` §4.3**（等 R25~R29）。
   期间**推翻 5 条 legacy 结论**（见 HANDOFF §2.3 第 7 条）
3. ✅ **Brinson 复核已完成**：`docs/个股线_行业中性化.md` 已全文重写，机制解释改为
   「中性化是**减少行业配置的伤害**、不是创造选股 alpha」；主结论仍成立
4. ✅ **全量回归通过**（`selftest` **24/0/0** + 文档命令审计四类全 0）
5. ✅ **提交推送已完成（2026-09-19）** —— `1b1c54f` → `c5343bb` → `8118368` 三个提交，
   已推送并核验远端 `refs/heads/master` = 本地 HEAD **`8118368`**；工作树 **0 未提交**。
   推送前脱敏：HEAD 仅 `share.bat:19` 的 `nokey@localhost.run` 隧道占位，无手机号/余额型数值

⚠️ 已知问题（详见 HANDOFF §五）：
- ~~**M2 组合层绝对值与定稿不一致**（9.26% vs 13.02%）~~ → ✅ **2026-09-18 已修复**（4 处口径 bug；
  修后与 `results/adj_correct_tax10_backtest.csv` **逐位对齐**，可引用）
- ~~个股线数据滞后~~ → ✅ **2026-09-18 晚已补齐**（`bars_bfq` 末根 20260915 → **20260917**），
  且**已确认 31 个产物不需要重跑**（`--end` 硬编码 `20260911`，刷新只往后延长、未改窗口内历史；
  `cmp_bars_snapshot` 实测三档「收益率差异 0 行」，R2 实跑 **18 行 × 7 指标最大差 = 0**）
- ~~9 份文档曾挂旧口径横幅~~ → ✅ **2026-09-18 已全部处理（15 份）**
- ⚠️ **correct 口径下「四进三出」结论大幅收窄**（只有样本外 +2.49pp；全区间 +0.69、样本内 +0.23），
  且**功劳分解反转**（门槛是负贡献）→ 见 `docs/个股线_四进三出阈值检验.md` 横幅
- ⚠️ **无税/税后两版不是「同一组合只扣税」**（换 `--bars` 会改选股）→ 长窗口的 Δ 不可读作税成本
- ⚠️ **`hyst` 与行业中性化不能叠加**：三段区间全部不可叠加、且低于「行业内单独」
- 个股线样本外仅 15 个调仓期 + 过滤器是 correct 口径下**负贡献** → **真实预期显著低于回测**
- ~~`落地路径与资金门槛.md` §11.2 有 3 行数字（`3e6/5e6/7e6`）找不到产物来源~~ →
  ✅ **2026-09-19 已复核**：3 行已补产物（`results/pool_params_ext.csv`），且查出
  **§11.2 / §11.3 整表属「修复前引擎」口径**（原产物均 09-15，早于 09-17 的两处口径级修复），
  与当前引擎差 **−0.24~−0.39pp**；**结论不变**（过拟合判定与口径无关）→ 见该文 **§11.5**

⚠️ 需要主人拍板（**不要自己决定**）：纸面跟踪名单是否重生成 / 个股线是否投钱 /
Faber 是否切换 / daily_job 连接重试 / 五闸门 PASS 后的规则切换。
（2026-09-19 已拍板两项：**`daily_job` 的 `last_run.json` 覆盖缺陷 → 选方案 C（`--manual`），
已实现并实测验证**；**资金规模「10 万」→ 选「加口径声明」，数字保留**，
声明见 `README.md` 顶部与 HANDOFF §1.3。）

✅ **2026-09-20 五项决策 D1~D5 已按授权推进完毕**（详见 HANDOFF §4.7）：
- **D4**：红利税改成**引擎内扣** → 税后与无税**组合恒等**（新 `tools/build_div_tax.py`
  + `tools/verify_div_tax.py`）。⚠️ **调用方尚未切换**，当前「税后 7.45%」仍是旧口径
- **D2**：纸面跟踪首期名单实测是 **legacy** 口径（20/20 vs correct 16/20）→ 已归档
  `state/paper_tracking/_legacy_20260915/`，按 `correct` **重设 anchor = 20260917**，
  并加了 `adj_mode` 元数据 + 口径守卫
- **D1**：Faber 门槛 **binding 率 47.0%**（新 `tools/faber_binding_diag.py`）→ 不能按噪音忽略；
  ⚠️ 影子表仍 **0 条**（只在调仓日记录，尚未跑到调仓日）→ 「8 周观察期」还没开始计时
- **D5**：`daily_job` 连接失败**限窗重试**（每 2 分钟 ×3、上限 14:56、只重试连接类）
- **D3**：**不投钱**，等首期完成（新 anchor 起算 → 约 2026-12 中）

✅ **D4 已全量落地**：调用方切换 + 重跑 R2/R3 + 回灌（见 HANDOFF §2.2 / §4.7）。
**结论：R2（无税）产物逐位不变；R3（税后）样本外几乎不变（7.4494%）、
全区间/样本内下移 0.21/0.41pp（税负由 −0.198 修正为 −0.412pp，旧口径低估了税）。**

⚠️ **下一步最该做的**（见 HANDOFF §4.7.4）：重跑其余 **8 个税后步骤**

```bash
$PY tools/run_reruns.py --only R6 R9 R10 R11 R12 R13 R23 R24 --force
```

⚠️ `--list` 会把全部 31 步都标「待跑」（因为 `INPUTS` 新增了 `div_tax_table`）——
**无税步骤不需要重跑**，R2 逐位不变就是证明。

环境约束（别重复踩）：
- 三个 Python 别混用：**刷新**用 E:\Python（64bit，akshare 在其用户 site-packages）；
  **下单**用 E:\Python32（32bit）；**研究/回测/自检**用托管 venv
  C:\Users\XiaoQi\.workbuddy-ai\binaries\python\envs\default
- 研究 venv **只有 pandas/numpy/pyarrow，没有 scipy/sklearn/lightgbm**。
  ⚠️ 因此 **不能**用 `pandas.corr('spearman')`（会转调 scipy 报错）→ 用 `tools/stats_lite.spearman`
- 沙箱里 import 失败先查 sys.path（shim 会改），别急着断言"依赖缺失"
- 宿主会**杀长任务**：前台 ≈12 分钟 SIGTERM、后台回合结束被杀 → 长任务用
  「后台启动 + 同回合轮询」或 `tools/run_reruns.py` 断点续跑
- 长任务输出**别用 `| head` 截**（SIGPIPE 杀进程、日志截断且无 Traceback）
- 写文件用 open(p,"wb")（Windows 文本模式会把 LF 变 CRLF，污染 git）
- 本机 Bash 需先修 PATH：export PATH="/c/Windows/System32:/c/Windows:/usr/bin:/bin:$PATH"
- ⚠️ 仓库是**公开的**：文档/记忆里不写账户金额、持仓数量、账号邮箱；
  **推送前必须 `git grep -nE '<模式>' HEAD` 扫敏感信息**

规矩（本项目很在意）：
- 诚实优先于好看。数据说不行就直说；说"验证过"必须真的跑过
- 可复现：留命令；数字要能追到「哪个文件、哪次运行」
- 严格验证：参数敏感性 / 样本内外 / **净成本**，缺一个都不算数
- **改完必跑 `tools/selftest.py`（27 项）**；文档改了命令要跑 `tools/audit_doc_commands.py`
- 会改研究结论口径的事（因子定义/复权/税率/成本假设）→ **先报备，不擅自替换既有结论**
- **有明确判断的事直接做完再汇报**，不要问"要不要做"；只有两类事前确认：
  ① 确实无法判断方向 ② 重大取舍与风险（推公开仓库、删数据、改实盘链路、改结论口径）

请先读完上面 6 份文档，然后告诉我你理解的当前状态和你会先做什么。
```

---

## 二、项目速览

### 两条线

| 线 | 状态 | 关键数字 |
|---|---|---|
| **ETF 轮动**（实盘） | 同花顺模拟盘，工作日 14:50 计划任务 | 3.33% / 夏普 0.47 / 回撤 −9.28%；**跑输等权买入持有 6.92pp** |
| **个股线·股息率**（研究定稿 + 纸面跟踪） | anchor=20260915 | 样本外（税后·10万真实）**7.45%** / 夏普 0.53 / 回撤 −26.13%；超额 **+3.42pp** |

### 关键模块

| 类别 | 模块 |
|---|---|
| ETF 实盘 | `main.py` / `scheduler/runner.py` / `trader/ths_uia.py` / `dataprovider/adjust.py`（读取时复权） |
| 个股研究 | `tools/backtest_stock.py`（引擎）/ `test_dividend_factor.py`（股息率面板唯一来源）/ `test_industry_neutral.py`（选股逻辑唯一来源）/ `backtest_dividend.py` |
| **自优化闭环** | `tools/eval_candidate.py`（M1 五闸门）/ `monitor_factors.py`（M2 监控）/ `walk_forward.py`（M3 DSR+PBO）/ `daily_learn.py`（M4 每日闭环） |
| 共享层 | `tools/costs.py`（成本唯一来源）/ `progress.py`（增量落盘）/ `panel_cache.py`（面板缓存）/ `stats_lite.py`（无 scipy 统计） |
| 质量保障 | `tools/selftest.py`（27 项）/ `audit_doc_commands.py` / `run_reruns.py`（31 步）/ `check_alerts.py` |

### 常用命令

```bash
cd "E:/MyWorkAndProject/量化/agentskill"
PY="C:/Users/XiaoQi/.workbuddy-ai/binaries/python/envs/default/Scripts/python.exe"

# 环境自检（改动前后都跑）
$PY tools/selftest.py
$PY tools/audit_doc_commands.py
$PY tools/check_alerts.py

# 看实盘
cat state/last_run.json
$PY main.py status
$PY main.py backtest --strategy etf_rotation --pool ETF全球 --topk 5 --rebalance 5

# 重跑 / 续跑
$PY tools/run_reruns.py --list
$PY tools/run_reruns.py --from R7

# 监控 / 候选评估 / 每日闭环
$PY tools/monitor_factors.py --adj-mode correct --strict
$PY tools/eval_candidate.py --factor-col dy_fwd --name cand1 --usage score --adj-mode correct
$PY tools/daily_learn.py --dry

# 数据追加（64 位）
"E:/Python/python.exe" tools/append_stock_bars.py --out data/stockbars --workers 8

# 送转口径回归（改动后必须重跑）
$PY joinquant/_verify_core.py
$PY tools/cmp_adj_mode.py
$PY tools/verify_adj_pointintime.py
```

### 定时任务

| 任务 | 时间 | 内容 |
|---|---|---|
| `QuantPanorama_DailyRun` | 工作日 **14:50** | `daily_run.bat`：`E:\Python` 刷行情+体检 → `E:\Python32` 下单 |

⚠️ 14:50 那一刻**同花顺客户端必须开着、已登录模拟盘、未最小化**（历史失败过 3 次）。
