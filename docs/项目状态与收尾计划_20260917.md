# 项目状态与收尾计划（2026-09-17 盘点）

> 盘点时间：2026-09-17 23:35 ｜ 仓库 `agentskill` @ `48175c7`（已推送）
> 本文回答：**哪些模块完工了、哪些没完工、风险在哪、怎么收尾**
> 核查方式：全部结论都有命令可复现，见 §六

---

## 零、一句话结论

**没完工。整体完成度 ≈ 80%。**

- **研究/工程主体已完成**（数据层、回测引擎、ETF 实盘线、个股线定稿、4 条证伪路线归档、质量保障体系）
- **没完工的全部集中在同一处：口径变更后的「数字回灌 + 文档定稿」**
  —— 9 份文档仍挂「旧口径已作废」横幅，7 步重跑（约 96 分钟）未跑
- **一条线在真跑**（ETF 轮动，模拟盘，每日 14:50）；个股线**只在纸面跟踪**，未上实盘

> 这个分布不奇怪：本项目的"完成"标准不只是代码能跑，还包括
> **每个数字都能追到哪次运行、且文档与实现一致**。回灌就是把最后这一层补齐。

---

## 一、模块状态总表

| # | 模块 | 开发 | 测试 | 部署 | 完成度 | 卡在哪 |
|---|---|---|---|---|---|---|
| A | **ETF 轮动线** | ✅ | ✅ 回测 + 真实前向 | ✅ 计划任务在跑 | **92%** | Faber 决策（等 ≥8 周影子数据） |
| B | **个股线·股息率** | ✅ | 🟡 回灌 7/9 待跑 | 🟡 纸面跟踪中，未上实盘 | **70%** | 响应口径回灌 + Brinson 复核 |
| C | 个股多因子线 | ✅ | ✅ | ❌ 有意不落地 | 研究 100% / 落地 0% | 10 万不可落地（已定论） |
| D | 短线个股线 | ✅ | ✅ 已证伪归档 | — | **100%** | — |
| E | 通达信社区策略线 | ✅ | ✅ 已证伪归档 | — | **100%** | — |
| F | 精灵大单因子 | ✅ | ✅ 双线证伪 | — | **100%** | — |
| G | **交易执行层（同花顺 UIA）** | ✅ | ✅ 模拟盘跑通 | ✅ | **90%** | 连接重试未做（已降级为备份方案） |
| H | **研究基础设施** | ✅ | ✅ | ✅ | **95%** | — |
| I | **文档 / 知识库** | ✅ | 🟡 | 🟡 | **75%** | 9 份挂横幅待回灌 |

---

## 二、逐模块证据

### A. ETF 轮动线 —— **唯一在真跑的线**

| 项 | 状态 | 证据 |
|---|---|---|
| 计划任务 | ✅ | `state/last_run.json`：**2026-09-17 14:50:08**，`ok:true` / 退出码 0 / `error` 空 |
| 告警 | ✅ | `state/` 下无 `ALERT.txt`、无 `DATA_ALERT.txt` |
| 数据新鲜度 | ✅ | 有内置守卫：数据不新鲜直接**拒单** |
| 成本口径 | ✅ | 佣金万5（`config.TRADING_COST`） |
| 份额折算 | ✅ | `dataprovider/adjust.py` + `DataStore.read()` **读取时自动复权**（根治"刷新回退"，实测 +0.98pp/年） |
| 数据体检 | ✅ | 挂在 `tools/refresh_data.py`，不过 → 写 `DATA_ALERT.txt` + 退出码 3 |
| Faber 并行验证 | 🟡 **等时间** | 框架就绪（`shadow_weights` 表 + runner 影子计算）；`SHADOW_ENABLED=True` / `TREND_LIVE=60` / `TREND_SHADOW=None`。**只在调仓日产生记录**，需 ≥8 周 |

**⚠️ 已量化的风险（不是 bug，是事实，必须一直写在报告里）**：
全区间 **3.33% vs 等权买入持有 10.25% → 跑输 6.92pp**；回撤 −9.28%（远小于基准 −45.1%）。
定位：**跑赢可投资指数，但跑输不可投资的等权全池**。

**⚠️ 外部硬依赖**：14:50 那一刻**同花顺客户端必须开着、已登录模拟盘、未最小化**。
历史已因这条失败过 3 次（09-14/15/16），09-17 恢复。

### B. 个股线·股息率 —— **定稿已成，但数字没回灌完**

| 项 | 状态 |
|---|---|
| 定稿规则 | ✅ `docs/个股线_股息率实盘方案.md`：全A池 →（0.5%≤dy≤10% + 近3年分红≥2次）→ **行业内百分位** → N=20 等权 → hold=60 → 每年 4 次 |
| 送转口径 | ✅ 已采纳 `correct`（7 条判据全过；`legacy` 差异恰好 0 vs 16.54% → legacy 是**不可复现**而非"偏高"） |
| 聚宽移植 | ✅ `joinquant/`（主文件 + 裸版对照 + 离线自检，与本地逐位一致） |
| **数字回灌** | 🟡 **R1/R2 已完成，R3~R9 未跑（≈96 分钟）** |
| **Brinson 复核** | ⚠️ **未做**，且**可能改结论**（见 §三·1） |
| 纸面跟踪 | 🟡 已启动（anchor=20260915），但**首期名单是 legacy 口径生成的** |
| 实盘 | ❌ 未上（需新增资金决策） |

**correct 口径下的定稿数字（无税，R2 实测）**：

| 区间 | 年化% | 夏普 | 回撤% | 10 万真实 |
|---|---|---|---|---|
| 全区间 | 13.360 | 0.758 | −26.630 | 13.096 |
| 样本内 | 14.422 | 0.775 | −23.331 | 14.154 |
| **样本外** | **8.112** | **0.533** | **−26.133** | **7.838** |

### C~F. 四条已证伪/未落地路线 —— **完成度 100%（含结论）**

- 个股多因子：`tilt` 构造 10 万下需 2,243 只 → **不可落地**（定论）
- 短线个股：样本内 18.04% → **样本外 −6.38%**，8 配置 OOS 全负
- 通达信社区策略：15 条全败，追涨程度与收益 **r = −0.896**
- 精灵大单：ETF 线 + 个股线**双线负贡献**

→ **这四条的价值就是"别再走"**，已归档，无需任何后续动作。

### G. 交易执行层

| 项 | 状态 |
|---|---|
| 同花顺 UIA 下单 | ✅ 模拟盘跑通（每交易日 14:50） |
| 下单前对账 | ✅ `run_once` 开头强制 `reconcile()`，失败**拒单** |
| 资金口径 | ✅ 冻结资金计入净值（防假回撤触发熔断） |
| 未做 | 🟡 连接重试（14:50 失败后重试）——**改的是实盘链路**，主人未批，已降级为备份方案 |

### H. 研究基础设施 —— **本轮刚打完地基**

| 项 | 状态 |
|---|---|
| 数据层 | ✅ `bars_total*`（不复权价 + 分红回放 + 真实成交额）、分红 37,779 条、行业 24,774 条、指数 |
| 回测引擎 | ✅ T+1 / 涨跌停（**本轮修好，漏判率 40.6% → 0**）/ 停牌 / 退市了结 / 最低佣金 |
| 成本模型 | ✅ **单一来源 `tools/costs.py`**（原 5 处重复且已分叉过一次） |
| 质量保障 | ✅ **新增**：`selftest.py`（13 项，13/13 通过）、`audit_doc_commands.py`、`run_reruns.py`（断点续跑） |
| 方法论 | ✅ `tools/README.md` 铁律 **15 → 19 条** |

### I. 文档 / 知识库

- 共 **33 份文档**；`tools/README.md` 脚本索引 **78 个脚本**
- **9 份仍挂「旧口径已作废」横幅**：`HANDOFF_项目总结与下一步` / `个股线_四进三出阈值检验` /
  `个股线_机制归因_hyst与行业中性化` / `个股线_红利税` / `个股线_股息率并入多因子` /
  `个股线_股息率策略` / `个股线_行业中性化` / `新会话开场提示词` / `项目运行手册`
- 另：`个股线_股息率策略.md` 的 IC 已可回灌（`results/adj_correct_ic.csv` 早在 20:41 就跑完了）

---

## 三、待完成清单

### 3.1 必须做完（**工程活，我来执行，无需决策**）

| # | 事项 | 预计 | 依赖 |
|---|---|---|---|
| 1 | **R3~R9 重跑**（`tools/run_reruns.py`） | ≈96 min | — |
| 2 | 回灌 6 份文档数字 + 摘横幅 | — | 1 |
| 3 | 模型回测（`test_stock_factors` / `validate_oos` / `sweep_scaling` / `sweep_feasibility`）——**换成本后重扫** | ≈40 min | — |
| 4 | 回归验证：`joinquant/_verify_core.py` / `cmp_adj_mode.py` / `verify_adj_pointintime.py` / `selftest.py` | ≈10 min | 1 |
| 5 | git 提交 + 推送（公开仓库，**推送前必须 `git grep -nE` 扫敏感信息**） | — | 2、3 |

### 3.2 ⚠️ 需要主人拍板（**我不擅自做**）

| # | 事项 | 为什么必须你定 |
|---|---|---|
| 1 | **行业中性化的 Brinson 结论可能翻转** | 见 §三·1，这是**结论级**变化，不是换数字 |
| 2 | 纸面跟踪 `anchor=20260915` 首期名单是否按新口径重生成 / 跟踪起点是否重设 | 改了会破坏跟踪的可比性 |
| 3 | 个股线是否投钱上实盘 | 回撤 −26.1% vs ETF 线 −9.3%，**风险特征差别很大**；若共用一笔钱要先想清总回撤承受度 |
| 4 | ETF Faber 是否切换 | 需 ≥8 周真实前向数据，现在只是框架就绪 |
| 5 | `daily_job.py` 连接重试 | 改实盘链路，风险不对等 |

### 3.3 只需等时间（**不需要写代码**）

| 项 | 等多久 | 怎么看 |
|---|---|---|
| 个股线纸面跟踪 ≥4 期 | ≈1 年 | `tools/paper_track_dividend.py --report` |
| Faber 并行验证 ≥8 周 | ≈2 个月 | `tools/faber_shadow_report.py` |

---

## 四、⚠️ 本次盘点点出的最大悬念（先说清）

### 4.1 `个股线_行业中性化.md` 的核心论断**在 correct 口径下不再成立**

R2 跑完后发现 Brinson 归因数字变了：

| 区间（indpct N=20） | 行业配置超额 | 选股超额 | 文档原论断 |
|---|---|---|---|
| 全区间 legacy | −1.69 | **+6.03** | 「超额**全部来自行业内选股**」 |
| **全区间 correct** | −3.21 | **−1.23** | ← **选股超额转负** |
| 样本外 legacy | −4.54 | +2.96 | — |
| **样本外 correct** | −3.27 | **+1.72**（中位 **−4.36**） | — |

**但主结论仍然成立**：`indpct − topn` 三区间增量 **+5.47 / +7.23 / +2.41pp**
（与送转文档判据⑥的 +5.38/+7.03/+2.43 吻合）→ **「行业中性化让组合变好」没变**。

→ **需要一次专门复核**：那份文档的"机制解释"部分要重写，**不能只换数字**。
   属"会改变研究结论口径的事" → **改之前报备**。

### 4.2 我上一轮报错过一个数字，已更正

「涨跌停现行判据只捕获 585 行」——**错**（当时用了全池统一 20% 的错误算式）。
正确：**现行 11,301 / 应有 19,023 → 漏判 7,722 行（40.6%）**，已修。

---

## 五、已知残留风险（**不可消除 / 已量化，必须随报告一起披露**）

| # | 风险 | 量级 / 性质 |
|---|---|---|
| 1 | ETF 线**跑输等权买入持有** | −6.92pp/年（全区间），已量化 |
| 2 | 个股线**过滤器是事后挑的** | `≤10%` 单独贡献 +8.64pp，是看过全区间才定的 → **真实预期显著低于回测**，只能靠前向数据定 |
| 3 | 个股线**样本外只有 15 个调仓期** | 置信区间很宽；单期（2026-03-27）占了一半拖累 |
| 4 | 行业分类是**当前标签，非 point-in-time** | 回测存在轻微前视；实盘无此问题 |
| 5 | **ST 的 5% 涨跌停未建模** | `universe.is_st` 是当前快照；池内中途转 ST 会漏判 |
| 6 | 退市了结 `writeoff_factor=1.0` | **偏乐观**，实际可能更低 |
| 7 | 红利税是**平坦税率近似** | 实际按持股期限差别化、FIFO 补扣；快速卖出会低估税负 |
| 8 | 实盘**外部硬依赖**：同花顺客户端 | 关机 / 未登录 → 当日直接失败（有告警链路） |
| 9 | **机器有死机史** | 已用「增量落盘 + 断点续跑」缓解（本轮新增 `run_reruns.py`） |
| 10 | 沙箱查不了计划任务 | `schtasks.exe` 被安全策略拦截 → 计划任务状态只能靠 `state/last_run.json` 间接验证 |

---

## 六、收尾计划（按序，每阶段可独立验收）

> ## 🔄 2026-09-18 执行进度（本节的实时状态）
>
> **P1 ✅ / P2 ✅ / P2b 🔄（R10~R18 在跑）/ P3 🔄（5/7 份已摘）/ P4 ✅ /
> P5 ⏸ / P6 🔄（`selftest --quick` 20/0/2）/ P7 ⏸（等主人批准）/ P8 ✅**
>
> 另：R10~R18 是 2026-09-18 **新补的缺口**（原清单只有 R1~R9）——
> 它们覆盖 `个股线_股息率实盘方案.md` 的 §三（N 敏感性）/ §四（hold 敏感性）/ §八（层层加码），
> 以及 `个股线_股息率策略.md` §二（IC）与 §三（无税阶梯）。
> **原清单的 R1~R9 根本没有这些步骤**，所以那几节长期停留在 legacy 数字。

| 阶段 | 做什么 | 验收 | 预计 |
|---|---|---|---|
| **P1** ✅ | `run_reruns.py --only R3 R4 R5 R6` | 4 个产物落盘 | 已完成 |
| **P2** ✅ | `run_reruns.py --only R7 R8 R9` | 3 个产物落盘（R8 最慢） | 已完成（R7 5.5 / R8 ~40 / R9 5 min） |
| **P2b** 🔄 | `run_reruns.py --from R10`（R10~R18，新增） | 9 个产物落盘 | ≈89 min |
| **P3** 🔄 | 回灌文档数字 + 摘横幅；IC 结果灌回 `股息率策略.md` | `grep -rln "旧口径（legacy）"` **应为空** | 已摘 5/7 份 |
| **P4** ✅ | **Brinson 复核** → 重写 `行业中性化.md` 的机制解释 | 结论与数字自洽 | 已完成（已全文重写） |
| **P5** | 模型回测换成本重扫（多因子线 4 个脚本） | 门槛矩阵更新 | ≈40 min |
| **P6** 🔄 | 全量回归：`_verify_core` / `cmp_adj_mode` / `verify_adj_pointintime` / `selftest.py` | 全过（退出码 0） | ≈10 min |
| **P7** ⏸ | 敏感信息扫描 → 提交 → 推送 | `git grep -nE '<模式>' HEAD` 干净 | **等主人批准** |
| **P8** ✅ | 更新 `HANDOFF_项目总结与下一步.md` + `新会话开场提示词.md` | 下一位接手者读它即可 | 已完成 |

**验收总判据**（全部满足才算收尾完成）：
1. `tools/selftest.py` 退出码 0
2. `tools/audit_doc_commands.py` 四类全 0
3. `grep -rln "旧口径（legacy）" docs/` 为空
4. `tools/run_reruns.py --list` 无「待跑」
5. 工作树干净且已推送到 `origin/master`

---

## 七、复现本次盘点

```bash
PY="C:/Users/XiaoQi/.workbuddy-ai/binaries/python/envs/default/Scripts/python.exe"
cd "E:/MyWorkAndProject/量化/agentskill"

git rev-parse --short HEAD && git status --short | wc -l   # HEAD / 未提交数
cat state/last_run.json                                    # ETF 线最近一次
grep -rln "旧口径（legacy）\|送转口径变更" docs/ tools/README.md   # 还挂横幅的文档
$PY tools/run_reruns.py --list                             # 回灌进度
$PY tools/selftest.py --quick                              # 质量自检
```

---

## 免责声明

研究与教学用途，不构成任何投资建议。所有回测数字均为历史模拟结果，**不预示未来收益**。
