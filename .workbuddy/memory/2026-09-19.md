# 2026-09-19 工作日志

> 本文件与 `E:\MyWorkAndProject\量化\.workbuddy-ai\memory\2026-09-19.md` 记录同一批工作，
> 此处为**仓库内留档的简版**（该文件在仓库外，不入库）。

## 一、本日主线：主人要求落实的三件事

| # | 事项 | 结果 |
|---|---|---|
| 1 | `config/monitor.json` 的 `decile_tb_min` | 1.0 → **−2.0**（理由见 `_变更记录`）；`monitor_factors.py --adj-mode correct` → **[正常] 全部指标在阈值内**，`state/LEARN_ALERT.txt` 自动清除 |
| 2 | 数据补齐的 `merge` + `rebuild_returns` | ✅ `bars_bfq` 末根 20260915 → **20260917**；三档 `bars_total*` 全部重建（9,439,204 行 / 5,457 只，归一化自检通过）；旧快照归档 `data/stockbars/_snapshots/` |
| 3 | 提交 + 推送 | ✅ **`1b1c54f`**（56 files, +8863/−1809），已 `git ls-remote` 验证远端与本地 HEAD 一致 |

## 二、⭐ 关键发现：数据刷新后 **31 个产物不需要重跑**

原以为 `merge` 会作废全部重跑产物（需重跑 ≈7 小时）。**实测推翻**：

1. **所有分析工具的 `--end` 默认值硬编码 `20260911`**，`run_reruns.py` 从不传 `--end`
   → 数据刷新只是**往后延长**，落在窗口之外
2. 新增 **`tools/cmp_bars_snapshot.py`**：比对两份 `bars_total*` 的**收益率**
   （不是价位 —— `rebuild_returns` 会把整条路径**整体缩放**，比价位会看到假差异）
   → 三档实测 **「收益率差异 0 行」**（最大差 8.9e-16 = 浮点噪声）
3. **实跑确认**：`--only R2 --force` 重算后 **18 行 × 7 指标全部最大差 = 0**
4. → **省下约 7 小时白跑**

**新增机制**：脚本写复核凭证 `results/_snapshot_verified.json`，
`run_reruns.py --list` 读它并标注「这些"待跑"是 **mtime 误报**，不必重跑」。

⚠️ 残留：**41 只股票的绝对价位被缩放**（偏离 >0.5%，最小 0.69）→
只影响 `min_price >= 2.0` 这类**价格类过滤**（已用 R2 实跑确认无影响）。

## 三、实盘告警 `state/ALERT.txt`：**根因已消失，故意不手工删**

- 09-18 21:02 一次**额外**运行失败：`trader/ths_uia.py:27` `import win32gui`
  → `ModuleNotFoundError`（当时 `pywin32` 正在装，属**瞬时**）
- **实测 `E:\Python32`（3.10.11 / 32bit）现在 5 个 win32 模块全部可导入** ✅
- 失败在 **broker 创建阶段**（`rebalanced: null`）→ **未产生任何交易**
- 09-18 **14:50 的计划任务那次是成功的**（`ok:true` / 退出码 0）
- ✅ `tools/daily_job.py` 第 **119-120** 行：**成功路径会 `ALERT.unlink()` 自动清除**
  → **下一次成功运行（周一 09-21 14:50）会自愈**
- **故意不手工删**：删了就丢证据、且可能掩盖复发

## 四、顺手解决的遗留项

- ✅ **`_rerun_R2.log` 已重建**（被 `docs/个股线_红利税.md` 引用但此前不存在）——
  跑 `--only R2 --force` 时自动生成；并借机验证**引擎无漂移**（重算前后逐位一致）
- ✅ **订正自己的一处失实陈述**：HANDOFF §4.1 第 6 行曾写「提交+推送已执行」，
  实际未执行 → 已订正。**教训：状态标记必须与 `git log` 对齐，别凭记忆。**

## 五、验证

- `selftest` **23/0/0**；`audit_doc_commands` A/B/C/D **全 0**
- `tools/*.py` = **88**（索引 100%）；`docs/*.md` = **38**（索引 100%）
- 脱敏：HEAD 与工作区均只命中 `share.bat:19` 的 `nokey@localhost.run`（公开隧道占位，无害）；
  手机号 / 余额型数值 **rc=1 无命中**；`state/` `data/` `results/` 均 **0 跟踪文件**

## 六、仍待

- `docs/落地路径与资金门槛.md` §11.2 有 3 行（`3e6/5e6/7e6`）找不到产物来源
  → 用 `sweep_pool_params.py --min-amounts 3e6,5e6,7e6,1e7,...` 复核
- 资金规模「10 万」是否从公开文档收回（主人一直未决）
- `min_div3 ≥ 3` 若要继续推：**只能用前向数据验证，不能再调参**（邻域检验已否决）

---

## 七、晚间追加（23:00~23:40）：三件事收尾 —— 提交 `8eb19e6`

> §六 待办里的前两项**已办**。

### 7.1 `state/ALERT.txt` 已处置（确认 + 归档 + 清理）

- **定性**：09-18 21:02 那次失败是 `win32gui` 缺失（pywin32 正在装，**瞬时**），**非代码缺陷**。
- **证据**：`E:\Python32` 下 win32 5 模块全可导入 ＋ `import trader.ths_uia` **整链成功**；
  失败在 broker 创建阶段（`rebalanced: null`）→ **未产生任何交易**；
  `daily_run.log` 原文显示**当天 14:50 计划任务那次成功**（`[daily_job] OK` / `[done] exit=0`）。
- **处置**：原件归档 `state/alerts_archive/`（含定性 `README.md`）；清除 `state/ALERT.txt`；
  按日志原文**还原** `state/last_run.json` 为 14:50 的成功记录（还原被覆盖的计划任务健康记录，非伪造）。
- **验收**：`E:\Python32\python.exe tools/check_alerts.py` → `[正常]` / **退出码 0**。
- **遗留（未修，待拍板）**：`tools/daily_job.py` **无条件覆盖** `last_run.json` →
  任何非计划运行失败都会污染当天计划任务的健康记录（**本次事故即此机制**）。
  A/B/C 三方案见 `state/alerts_archive/README.md`；属实盘链路改动，**未擅自实施**。

### 7.2 工作树

**干净、0 未提交**。09-18 文档写的「未提交 47 项」已过期（那批已在 09-19 分三个提交推送）。
另修了一个环境问题：`origin/master` tracking ref 丢失（`git status` 显示 `[gone]`）
→ `git fetch origin` 重建，核验远端 `refs/heads/master` = 本地 HEAD。

### 7.3 `落地路径与资金门槛.md` §11 复核（§六 第 1 项）

- **3 行已补产物**：`results/pool_params_ext.csv`（`3e6/5e6/7e6/1e7` = 18.47/18.33/18.10/17.91）
- **查出更根本的问题**：**§11.2 / §11.3 整表是「修复前引擎」口径** ——
  原产物均生成于 **09-15**（`pool_params.csv` 17:38 / `oos_*.csv` 17:43·19:00），
  早于 09-17 引入、09-19 提交（`1b1c54f`）的两处口径级修复（涨跌停判定重写 + 成本自洽）
  → 与当前引擎 **系统性差 0.24~0.39pp**
- **归因硬证据**：`data/stockbars/bars_all.parquet` mtime = **09-12**（未变动）
  → 数据未变，**差异只能来自引擎**
- **结论不受影响**：样本外 `1e7` 比 `3e7` 差 **−1.24pp，修复前后逐位一致**；
  样本内 `1e7` 仍 +3.62pp 占优 → **过拟合判定与引擎口径无关**
- **文档落笔**：§11.2/§11.3 顶部**口径横幅（含修复前后对照表）** + §11.4 补说明
  + **新增 §11.5「口径复核与复现命令」**
- 附带：本节数据底是 `bars_all.parquet`（**`qfq` 有偏口径**），属个股多因子线 legacy；
  该线已定论「10 万下不可落地」→ **本节参数选择不影响任何实盘决策**，复核目的是**文档可信度**

### 7.4 验证与脱敏

`selftest` **24/0/0** ＋ `audit_doc_commands` **A/B/C/D 全 0**（命令数 174 → 177）
＋ `check_alerts` 退出码 **0**；工作区 diff 脱敏扫描（真实金额型数字 / 手机号 / 账号）**全空**；
`state/` `data/` `results/` 跟踪文件**各 0**。

### 7.5 仍等主人拍板（本轮新增 2 项）

- **`daily_job` 的 `last_run.json` 覆盖缺陷**是否修（A/B/C 三方案）
- **资金规模「10 万」是否从公开文档收回** —— 事实基线：**325 处命中 / 66 个文件**
  （含文档名 `落地决策_资金10万.md`、`config.py` 注释、38 份文档中的 20+ 份、memory 日志、
  joinquant 移植文档）；性质是**建模资金档位**（最低佣金惩罚建模的前提），**非账户余额/账号**；
  且**已处于公开状态**（09-19 已推送）→ 「收回」只能防未来、不能防过去。
  → 已写入 **HANDOFF §4.6 第 7 项**，三选项：① 收回 ② 加口径声明 ③ 判定可接受
