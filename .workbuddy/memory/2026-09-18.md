# 2026-09-18 工作日志

## 一、状态核查（09:08 前后）
- **ETF 实盘**：09-18 14:50:09 `ok:true` / 退出码 0 / 无 ALERT、无 DATA_ALERT → **连续 2 天干净**
- **重跑**：R1/R2 昨晚完成，R3 于 23:42 完成，R4~R9 待跑
- `git`：HEAD `48175c7`，未提交 33 项

## 二、产出 `docs/项目路线图_长期迭代.md`（本轮主交付）
长期迭代路线图，含：现状速览 / **六条设计原则**（单一来源、静默是 bug、一次只动一个变量、
参数必须证明 binding、数字可追溯、改动前后跑自检）/
**三大主线** A 数据更新 · B 策略扩展 · C 回测与实盘衔接 /
**P0~P4 阶段 + 优先顺序 + 可执行验收标准** / 长期节奏（日/周/季/半年）/ 已知风险 10 条。

关键结论：
- **B 只找新信息源，不找新规则**（~11% 是当前信号质量天花板）；候选排序
  `dy_fwd 事件型` → 财务质量类 → ETF 扩池（21+）→ 分钟级数据（最后）
- 每个候选过**五闸门**：样本内外同号 / 邻域同向 / 净成本为正 / 增量非冗余 / 机制可解释
- `dy_ttm` 已经试过且否决（正交 ≠ 有增量），不得重复走
- **退出条件先定**：个股线前向连续 4 期低于中证1000 或偏离 >5pp 且无解释 → 暂停并重新归因

## 三、实施 P1 的两项「完善」

### A1 增量落盘 ✅（这是本项目最实在的稳定性缺口）
新增 **`tools/progress.py::flush_partial(rows, out, tag)`**（覆盖写、**幂等**、单一来源），
接入 **4 个长循环脚本**：`sweep_hyst` / `test_industry_neutral` / `sweep_dividend_into_mf` /
`diag_dividend_into_mf` —— 每跑完一个区间就落盘。

**动机是实测**（不是推测）：09-18 前台跑 R4，宿主在 **12 分钟处 SIGTERM**，
`sweep_hyst` 原本只在结尾写一次 CSV → **12 分钟白跑、`results/` 里连半成品都没有**。

### A3 个股线数据新鲜度守卫 ✅
原先**只有 ETF 线**有新鲜度守卫，个股线没有 → 研究/纸面跟踪可能跑在陈旧数据上。
已加入 `tools/selftest.py`：比对「个股线数据末根交易日」vs「最近已收盘交易日」，
落后 >10 自然日**直接失败**、>4 日给警告。当前实测：末根 `20260915` vs 基准 `20260917` → 落后 2 日（未越线）。
selftest 现在 **12 项 quick / 14 项 full**。

## 四、⚠️ 环境限制（本轮再次确认，很重要）
1. **前台命令有时限**：实测约 **12 分钟处被 SIGTERM**（R4 首次尝试即死于此）
2. **后台任务在 agent 回合结束时被杀**（Batch A 的教训）
3. → **长任务唯一可行路径 = 后台启动 + 同一回合内用 TaskOutput 轮询保活**

## 五、R4~R6 后台运行中
`tools/run_reruns.py --only R4 R5 R6`（后台 task，同回合轮询保活）。
实测 `sweep_hyst` 每个区间约 12 分钟（18 配置 × ~40s/个）→ R4 实际约 36 分钟（不是清单里估的 15 分钟，
**`run_reruns.py` 的耗时估计偏乐观，应修正**）。

## 六、R4/R5/R6 全部完成（后台 + 同回合轮询保活，实测 55.5 min）
| 步 | 内容 | 实测 | 产物 |
|---|---|---|---|
| R4 | 四进三出阈值网格（无税） | **29.7 min**（原估 15 → 已修正） | `results/adj_correct_hyst_sweep.csv` |
| R5 | 纯门槛对照 | 6.8 min | `results/adj_correct_hyst_ctrl.csv` |
| R6 | 四进三出（税后） | **19.0 min**（原估 8 → 已修正） | `results/adj_correct_hyst_sweep_tax10.csv` |

**A1 增量落盘在生产中验证通过**：R6 日志出现
`[进度] 已增量落盘 11 行（刚完成：全区间）` → `22 行（刚完成：样本内）`
→ 即使中途被杀也保住 2/3 区间。

### 样本外结果（无税，correct + 新引擎）：**legacy 结论方向全部保持**
| 配置 | 年化% | 夏普 | 回撤% | 平均持仓 | 10 万真实 |
|---|---|---|---|---|---|
| 基线 topn | 5.70 | 0.40 | −24.30 | 19.98 | 5.42 |
| **hyst 8/4** | **8.19** | **0.53** | −24.89 | 19.98 | 7.95 |
| hyst 8/5 | 8.24 | 0.53 | −25.06 | 19.71 | 8.00 |
| hyst 8/6 | 8.87 | 0.56 | −24.72 | 18.84 | 8.65 |
| hyst 8/7 | 5.93 | 0.41 | −24.64 | 18.24 | 5.71 |
| hyst 9/5（持仓塌陷） | 10.91 | 0.69 | −19.27 | **13.84** | 10.85 |

**四条结论逐条对上 legacy**：
1. **`entry=6/7` 的 6 个配置与基线逐位相同**（5.70/0.40/−24.30/19.98/2.79）→ **门槛仍不 binding** ✓（铁律 8）
2. `entry=8` 仍是**生效边界**（7 不生效、8 生效）✓
3. `entry=9` 持仓塌到 **9.7~15.6** → 仍不可用 ✓
4. `hyst 8/4` 样本外增量 **+2.49pp**（legacy +2.76pp）→ 同量级；回撤与基线几乎相同（−24.89 vs −24.30）✓

### 已修正 `run_reruns.py` 的耗时估计（原估偏乐观 2 倍，会误导排期）

## 七、剩余
- **R7（并入多因子，13 min→实测可能 30+）/ R8（深挖，40 min）/ R9（税后回测，5 min）**
- P0-2 文档回灌（四进三出这份现在**数据已齐**，可立即写）
- P0-3 Brinson 复核（**需报备**）
- 当前 selftest：**quick 14 项 / full 16 项**，全过

## 八、产出 `docs/个股线_收益归因与优化路径.md`（回答「收益太低？能否回测自优化？」）
**结论：7.45% 不算低（跑赢中证1000 约 3.42pp），但靠回测调参提升不了 —— 已被证伪 5 次。**

### 偏低原因分解（4 条，带数字）
1. **信号本身弱（主因）**：60 日 IC **0.0553**（correct）＜ 现有 `vol20` 的 0.0935；
   样本内→样本外 0.0668→0.0410（衰减 39%）；**分年度仅 5/8 为正**（2021/2024/2025 为负）
2. **事后选择偏差（第二大，回测里消除不掉）**：裸版全区间 6.82% **跑输中证1000（−0.23pp）**；
   `≤10%` 上限单独贡献 **+8.64pp**，它与"行业内排序"都是看过全区间才挑的
3. 成本+税 ≈ **0.6~0.7pp/年**（最低佣金 0.28 + 税 0.29~0.39）
4. 样本外仅 **15 期**，单期（2026-03-27）占一半拖累 → 置信区间宽

### 已证伪 5 种"自优化"做法（别重走）
参数网格再搜（N/hold 是噪声参数，波动 3~4pp）｜短线追强势股（样本外 −6.38%）｜
`dy_ttm` 入多因子（−1.76pp）｜`hyst` 阈值（样本外与 indpct 抵消 −0.64pp）｜压缩持仓/提高流动性门槛

### 可行方向（换信息源，不换规则；预期 1~3pp）
`dy_fwd` 事件型（零新增成本，已在面板）→ 财务质量类 → 股息率×低波动（主要改夏普/回撤）
→ ETF 扩池 21+ → 分钟级数据（最后）
**加法之一：资金 10 万→20 万，单笔 1 万 → 有效佣金率回到万5，白赚 0.28pp/年。**

### 「自优化」的正确工程形态
不是自动调参，而是 **`tools/eval_candidate.py`：输入候选 → 自动跑五闸门 → PASS/FAIL + 证据表**；
FAIL 的写进已证伪清单，下次不许重走。**目标是收敛（越筛越准），不是发散。**

### 退出条件（先定）
纸面跟踪连续 4 期 < 中证1000，或前向与回测偏离 >5pp 且无解释 → **暂停该线**，不追加资金。

### 遗留
- **裸版在 correct 口径下尚未重跑**（文档里那张"层层加码"表仍是 legacy）→ **应补一次**
- `tools/eval_candidate.py` **只有规格，未实现**（需 ~200 行 + 测试）

## 九、联网查证「7.45% 低不低」→ 产出 `results/benchmark-comparison-20260918.html`

### 结论（可复用的判断）
**7.45% 不低到亏钱，但作为因子策略偏弱。** 分层看：
| 层 | 参照 | 本项目 |
|---|---|---|
| 可投资指数 | 中证1000 近5年 1.38% / 近10年 **−1.76%** | **+3.42pp** ✅ 通过 |
| **同赛道（真正的对标）** | 公募1000指增超额 **11.06%**、私募1000指增超额 **18.58%**（2018-2025） | 只有 **1/3~1/5** ❌ 明显偏弱 |
| 偏股基金长期 | 万得偏股混基长期年化 **12.67~12.89%**；近3年 10.23% | 低于它约 3~5pp |
| 个人投资者平均 | 散户五档年化 −4.03/−2.55/−1.60/−0.90/**+1.05%** | 远高于它 |

**"职业炒股人普遍高于 7.45%" 不成立**：学术 + 券商数据都显示个人主动交易平均为负。

### 权威数据（备查，含来源）
- **Jan 2022 JME 顶刊**：An, Bian, Lou & Shi《Wealth Redistribution in Bubbles and Crashes》——
  上交所 **4000 万账户**行政数据：2014-07~2015-12，**后85%散户主动交易 18个月 −28%**、
  前0.5% **+31%**；平静期两组差距仅 1~3%。
- 东方证券《量化基金2025回顾及2026展望》(2026-03-03)：2018-01~2025-12 平均**年化超额**——
  公募 300/500/1000 指增 3.90/4.88/**11.06%**；私募 6.61/8.81/**18.58%**（朝阳永续）。
- 私募量化多头 2019 以来年化 **14.4%**（至2026-04-24）；2025 单年私募1000指增 172 只平均收益 **49.78%**、超额 17.49%（私募排排网）。
- 中证指数公司（2026-09-15）：中证1000 近1年 +0.30%、近3年年化 **6.96%**、近5年年化 **1.38%**，年化波动 25%+。
- GuruFocus（2026-09-11）：中证1000 3年年化 +6.30%、5年 **−1.31%**、10年 **−1.76%**。
- 万得偏股混基指数：基日以来年化 12.67%（Wind, 至2026-05-06）/ 12.89%（国金, 至2026-06-30）；近3年 10.23%。
- 上交所《2025个人投资者行为分析报告》（二手转述，可信度中）：持仓<1个月散户年化换手 >1200%、平均 −18.7%；
  月换手>100% 年度亏损率 91.3%；换手最高前10% 年化 **−19.7%** vs 最低后10% **+6.2%**。

### ⚠️ 引用纪律（新增）
1. **中证1000 的"跑赢"含金量有限**：近10年年化 **−1.76%**，跑赢一个长期不涨的基准不算本事
2. **区间敏感**：同一指数近3年 6.96% / 近5年 1.38% / 近10年 −1.76%，差 **8.7pp** → 必须写起止区间
3. **口径不可混用**：公私募的"年化超额"是相对基准，本项目 7.45% 是**绝对收益（税后·10万真实）**
4. **私募样本有幸存者偏差**（只统计"有业绩展示"产品，且多用算术平均）
5. **2025 是特殊年份**（1000指增 49.78%）不可外推为长期中枢
6. 雪球/头条类聚合数字（如"2026H1 散户平均 −23.6%"）**未找到原始出处**，只作参考不作依据

## 十、产出 `docs/因子策略自优化方案_每日学习闭环.md`
回答「因子策略短板 + 设计每日学习持续自优化方案」。

### 五个短板（都有数字支撑）
1. **信息源太单一（根因）**：有效信息只有「股息率 + 8 个价格量因子」，**同源同族**；
   股息率 IC 0.0553 还低于 `vol20` 的 0.0935；`dy` 与 8 因子正交却样本外 −1.76pp（正交 ≠ 有增量）
2. **无风险模型（次因，最便宜的改进）**：无市值/波动/流动性中性化 → 8 因子约 **11% 收益来自风格暴露**；
   行业暴露漂移**无监控**（hyst×indpct 抵消就是证据）
3. **验证框架有结构缺口**：60 日标签 + 60 日调仓 → **标签高度重叠**（有效样本远小于名义样本、t 值被高估）；
   样本内外是**日历硬切**、无 purged/embargo；已搜 28+24+6 个配置却**从未做 DSR/PBO 校正**
4. **无自适应与降级**：参数定死；IC 从 0.055 掉到 0.02 不会有任何提示；分红 **5/8 年为正**、整年失效照买不误
5. 工程侧：个股线数据落后 2 日无自动补；分红/行业手动抓；无自动候选评估

### 方案骨架（关键取舍）
- **目标分三层**：L1 监控（**每日**，只产状态/告警）｜L2 数据（**每日**）｜L3 模型（**月度/季度 + 前向验证**）
- **红线：每日闭环不得修改已定稿规则**
- **"每日学习"的正确形态 = 特征增量 + 标签回填 + 监控**，**不是每天重训模型** ——
  因为 60 日标签下每天新增有效样本只有 1/60，每天重训 = 纯噪音放大
- **频率**：日（数据+监控）/ 周（周报+拥挤度）/ 月（五闸门评估）/ 季（影子对比+**人工确认**才能切换）/ 半年（全量回灌）
- **纪律**：**变更的"评估"可自动化，"生效"必须人工确认**（本项目三次大坑都是自动/静默变更）

### 评估指标三层 + 过拟合专用
策略层（样本外 7.45%、超额 +3.42pp、滚动12月 IC ≥0.02、回撤 ≥−30%、前向偏离 ≤5pp）／
因子层（RankIC/ICIR/分年正比例 5/8/单调性/衰减/拥挤度/换手）／组合层（行业 HHI 0.050、行业数 20、市值分位、单行业 ≤15%）／
**过拟合专用：DSR、PBO、purged+embargo walk-forward、参数平台**（本项目从未做过）

### 实现方向（7 个模块，按收益÷成本排序）
1. `tools/eval_candidate.py`（最高优先，五闸门自动化）
2. `tools/monitor_factors.py`（让失效当天可见）
3. `tools/walk_forward.py`（DSR/PBO）
4. `tools/daily_learn.py`（串起来）
5. **风险模型中性化**（最确定的一笔收益）
6. 新数据源（财务质量 → 资金流 → 事件）
7. `tools/weekly_data.py`

### 诚实边界
- **~11% 是当前信号质量天花板** → 本方案的**主要价值是"不再变差 + 更早发现失效"**，不是把 7.45% 变 20%
- 每日学习改变不了"弱信号"本质；样本外仅 15 期 → 任何提升必须前向复核
- **只写了设计，`eval_candidate.py` 等 7 个模块均未实现**

## 十一、产出 `docs/需求规格_自优化闭环模块.md`（**交付给实现方的规格书**）
主人要"详细说明任务需求，他会据此实现"→ 我写成可实现规格书，接口/判据全部定死。

### 范围：M1~M4
| 模块 | 目标 | 关键验收 |
|---|---|---|
| **M1 `eval_candidate.py`** | 输入候选因子 → 自动跑**五闸门** → `PASS/FAIL/REVIEW` + 证据表 | 自检用例（拿 `dy_ttm` 当候选）**必须 FAIL**（\|ρ\|=1.0）且 `state/refuted.jsonl` 追加一行 |
| **M2 `monitor_factors.py`** | 策略失效**当天可见** | 反例测试：阈值改 99 → `exit=3` + 写 `state/LEARN_ALERT.txt` |
| **M3 `walk_forward.py`** | purged+embargo 切分 + **DSR / PBO** | `PBO∈[0,1]`、`DSR` 有限、**无 NaN** |
| **M4 `daily_learn.py`** | 一条命令完成「数据增量+特征增量+标签回填+监控」 | 连跑两次，第二次各步 `skipped`（幂等） |

### 硬约束（写进规格 §2、§6）
- **解释器**：托管 venv（3.13）；**只有 pandas/numpy/pyarrow** —— ❌ 无 scipy/sklearn/lightgbm
  → Spearman 用 `pandas.corr('spearman')`；正态 CDF 用 `math.erf` 自实现；**不许加依赖**
- `--adj-mode` 必传（`require_adj_mode`）；成本只用 `tools/costs.py`；年化必须给「税后 10 万真实」
- 长循环必须 `flush_partial` 增量落盘；**面板必须缓存**（key = 数据 mtime+size+adj_mode+price_col 的 sha1 前12位）
- 单次命令 ≤10 分钟（宿主会 SIGTERM）；禁止 `| head`
- 写文件用 `open(p,"wb")`/`newline=""`（防 CRLF 污染 git）；公开仓库**不写账户金额**
- **红线**：M1~M4 不修改任何已定稿规则、不写 `trading.db`、不下单；只 import 既有脚本，不修改其行为

### M1 五闸门判据（定死，实现方不得改）
① 样本内外同号（Δ>0 且同号）② 邻域 ≥3 点样本外 Δ 全同号且 >0 ③ `10万真实Δ>0` ④ 与 8因子+dy 的 max\|ρ\|<0.5 且 Δ>0
⑤ **只输出暴露差异表 → 给 `REVIEW`/`FAIL`**（自动化不定论，PASS 需人工确认）
总判定：①∧②∧③∧④ PASS 且 ⑤=REVIEW → `PASS`（进影子期）；任一 FAIL → `FAIL` 并记入 `state/refuted.jsonl`

### 交付物 8 项
M1~M4 + `config/monitor.json`（12 个阈值键）+ `tools/selftest.py` 追加「自优化」6 条检查 + `tools/README.md` G节4条+铁律20

### 需主人确认 5 件事
① 锁箱区间（建议 2026-01-01~2026-09-11 锁死）② M2 阈值用默认 ③ 不允许加依赖 ④ 顺序 M1→M2→M3→M4 ⑤ 一模块一 commit + 推送前扫敏感信息

## 十三、执行规格书：M1~M4 全部实现并验收（主人要求"完整实现"）

### 交付 9 个文件（规格 8 项 + 2 个规格外共享层，另 selftest/README 已改）
| 文件 | 内容 |
|---|---|
| `tools/eval_candidate.py` | M1 五闸门评估器（~460 行）→ `PASS/FAIL/REVIEW` + 证据表 + `state/refuted.jsonl` |
| `tools/monitor_factors.py` | M2 健康监控（~375 行）12 指标 + `LEARN_ALERT.txt` + `--strict` exit 3 |
| `tools/walk_forward.py` | M3 purged+embargo + DSR + CSCV-PBO（~295 行） |
| `tools/daily_learn.py` | M4 四步闭环（~290 行，当日状态文件保证幂等） |
| `tools/panel_cache.py` | **规格外**：面板构建+指纹缓存（M1~M4 共用） |
| `tools/stats_lite.py` | **规格外**：无 scipy 的 Spearman / 正态 CDF+分位数 |
| `config/monitor.json` | 12 个阈值键 |
| `tools/selftest.py` | +6 条「自优化」检查（现 20 项 quick 全过） |
| `tools/README.md` | G 节 +6 条目、铁律 19→20 |

### ★ 规格里的一处硬伤（已指出并给出默认处理）
**§2.1 说「Spearman 用 `pandas.Series.corr('spearman')`」→ 实测不可行**：
本环境 pandas 内部 `from scipy.stats import spearmanr` → `ModuleNotFoundError`（venv 无 scipy）。
第一次跑 M1/M2/M3 **三个模块全崩**。
→ 处理：新增 `tools/stats_lite.py`，用「**秩的 Pearson 相关**」自实现（定义等价），
  正态 CDF/分位数用 `math.erf` + 二分法（验：`nppf(0.975)=1.959964` ✓）。
**另 4 处缺口**（§十一 已记）：缺共享面板层 / `plan_selections` 需可选扩展点 /
`monitor.json` 规格说 12 键只列了 10 个 / `--n-trials` 必须人工如实给。

### 验收结果
- **A1 selftest 20/20 quick 通过**（含 6 条新检查）
- **A3 M1**：`verdict=FAIL`、`gate4=False`、**`max|ρ|=1.0000`**、写入 refuted ✓（正是设计意图：拿 dy 自己加自己必被抓）
- **A4 M2 正例**：exit=0；IC(全) 0.0525 / ICIR 0.449 / IC(内) 0.0631 / IC(外) 0.0371 / 近12月 0.0563
  / 分年度为正 **7/8** / 十分位顶底 +1.33pp / 单调占比 0.333（**非单调**，与既有结论一致 ✓）
  / 行业HHI 0.0391 / 换手 2.65x；**全部在阈值内**
- **A5 M2 反例**：阈值改 99 → **exit=3** + `LEARN_ALERT.txt` 写出（含处置建议）、config 已还原 ✓
- **A6 M3**：5 折、purge+embargo **120 天**、OOS IC +0.1243、**5/5 折为正**、
  **DSR=1.0**（SR0=0.388，用 9 因子的实际 Var(SR)=0.036）、**PBO=0.01**、无 NaN ✓
  （原实现把 Var(SR) 假设为 1.0 → SR0 虚高、DSR 恒为 0 → 已改为用实际方差，**有区分度**）
- **A7 M4 幂等**：跑中（见 `results/_verify_a7.log`）

### 我修的两个自身 bug（都记进代码注释）
1. `forward_dev` 取了 CSV **第一列**（是 `date`）当代码 → 表现成"名单无匹配"，
   正是**铁律 7**「主键格式不对齐会伪装成数据源没覆盖」。已改为按列名 `code` 取。
2. `panel_cache` 里 `ind_p` 变量作用域错误（定义在 `fingerprint` 里却在 `build_panel` 用）→ 已修。

### ⚠️ 已知遗留（不掩盖）
**M2 的组合层绝对值与既有定稿不一致**：M2 首跑 全区间 年化 **9.26%** / 回撤 −26.00%，
而 R3 定稿为 **13.02%** / −26.92%。疑因：① 个股线数据文件今日已更新（9,022,750 → **9,032,766** 行）
② `industry_exposure` 的口径可能与逐调仓日口径不同。
→ **对齐前 M2 的绝对值不可引用**（相对变化/越界判定不受影响）。

## 十四、A7 完成 + M4 又两个幂等 bug 已修（最终状态）
- **M4 幂等 bug**：步骤②命中缓存时 `return ..., None` → ③④ 拿不到面板 → 第二次必中止。改为"跳过时仍读缓存"（1 秒）。
- **M4 状态判定过严**：skip 条件 `status=="ok"`，而上次记的是 `"skipped"` → ①②会重跑。放宽为 `in ("ok","skipped")`。
- **A7 最终结果**：第一次四步全 ok（标签回填 **96.5%**，最后已到期日 20260623）；
  第二次 **step1~4 全 skipped、exit 0、0.0 min**。
- **最终自检**：`selftest.py --quick` **20/20 通过**；git 未提交 47 项。

### 验收全表（A1~A7 全部通过）
| A1 | selftest 20 项全过 | ✅ |
| A2 | 文档命令审计四类全 0 | ✅ |
| A3 | M1 拒绝冗余候选（max\|ρ\|=1.0000 → FAIL + refuted） | ✅ |
| A4 | M2 正例 exit=0 | ✅ |
| A5 | M2 反例 exit=3 + LEARN_ALERT | ✅ |
| A6 | M3 DSR=1.0 / PBO=0.01 / 无 NaN | ✅ |
| A7 | M4 幂等（第二次全 skipped） | ✅ |

**M1 实测 ≈4 分钟（缓存命中），M2 ≈45 秒，M3 ≈1.7 分钟，M4 ≈1 分钟** ——
全部远快于规格预估，原因是有 `panel_cache`（首次建面板约 1.5 分钟，之后命中即 1 秒）。

## 十五、产出交接文档（覆盖重写 HANDOFF + 开场提示词）

### 1. `docs/HANDOFF_项目总结与下一步.md` **整体覆盖重写**（唯一入口文档）
按主人要求的 7 个板块组织：
① 背景与目标（两条线 + 6 条硬约束）② 进展与完成情况（模块完成度表 + 两条线关键数字 + 近两日做了啥）
③ 核心功能与模块（目录结构 / ETF 线 / 个股线 / M1~M4 / 质量保障 / 每日链路）
④ 待办与计划（P0 收尾 / P1 加固 / P2 前向 / P3 扩展 / P4 资金 / **5 项待拍板**）
⑤ 已知问题与风险（当前问题 5 条 / 口径方法残留 8 条 / 工程环境坑 8 条）
⑥ 环境与依赖（三个 Python / 依赖边界 / 数据文件含末根日 / 关键配置 / 计划任务）
⑦ 联系人与协作（**不写个人信息**，指向本地配置与 memory；6 条协作约定）
+ §八 接手后前 3 小时动线（可直接照跑的命令）
+ §九 文档地图 + **旧版内容去向表**（旧 HANDOFF 的 16 条坑 → tools/README 铁律；旧版全文 → `git show 48175c7:...`）

**关键交接信息**：整体完成度 **≈80%**、HEAD `48175c7`、**未提交 47 项**、
规模 **86 脚本 / 38 文档 / 222 结果文件**、R7~R9 剩 **58 分钟**。

### 2. `docs/新会话开场提示词.md` 同步重写
阅读清单 7 份 → **6 份**（去掉已合并的）；项目状态更新为 correct 口径；
**新增"不能用的写法"**：`pandas.corr('spearman')`（venv 无 scipy）；
新增 M1~M4 命令；已知问题与待拍板项同步。
⚠️ 顺手修正旧版的两处过时/错误表述：聚宽环境（旧版写 Py2，实为 **Py3 + 老 pandas**）、
"晨间 14:50 故障已恢复"的表述改为状态陈述。

### 3. 公开仓库脱敏自查（**我自己刚写的文件也必须扫**）
- 邮箱模式：**无** ✓
- 账户金额/持仓数量：**无** ✓
- `config/mail_alert.json`（含授权码）**未被跟踪** ✓（`.gitignore` 已覆盖，`git check-ignore` 验证通过）
- `HEAD` 全库扫邮箱：唯一命中 `share.bat:19` 的 `nokey@localhost.run` —— 那是
  **localhost.run 公网隧道的固定匿名用户名**（语法如此），**不是个人邮箱**，无需处理
- ⚠️ 待提交时注意：`config/` 目录目前**未被跟踪**，提交会带上 `monitor.json` 与 `mail_alert.example.json`
  （**正确**）；但**永远不要** `git add -f config/mail_alert.json`
